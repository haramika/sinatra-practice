<h1>メモアプリ</h1>
<a href="/memos/new">追加</a>
<ul>
  <% if @memos.empty? %>
    <li>
      <p>メモがありません</p>
    </li>
　<% else %>
    <% @memos.each do |id, memo| %>
    <li>
      <a href="/memos/<%= id %>">
      <%= h(memo['title']) %>
    </li>
    <% end %>
  <% end %>
</ul>
